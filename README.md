# bigcompany-org-analyzer
A Java 17 command-line application that reads an employees CSV, validates manager salary ranges versus direct reports, identifies excessively long reporting lines (>4 managers to CEO), and reports violations.
